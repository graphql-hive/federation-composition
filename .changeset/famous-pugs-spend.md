---
"@theguild/federation-composition": patch
---

Further improve whitespace formatting and reduce overhead for `addInaccessibleToUnreachableTypes` and `composeSchemaContract` by operating on already parsed documents instead of parsing the sdl.
